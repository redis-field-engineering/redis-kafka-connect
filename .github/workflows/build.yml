# https://github.com/redis-field-engineering/redis-github-workflows/blob/main/.github/workflows/build.yml
name: Build

on:
  pull_request:

jobs:
  build:
    name: 'Build'
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: 'zulu'
          java-version: "21"

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: ~/.gradle/caches
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle') }}-${{ hashFiles('**/gradle.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-
      
      - name: Cache Gradle wrapper
        uses: actions/cache@v4
        with:
          path: ~/.gradle/wrapper
          key: ${{ runner.os }}-gradlew-${{ hashFiles('**/gradlew') }}
          restore-keys: ${{ runner.os }}-gradlew-

      - name: Build with Gradle
        run: ./gradlew build aggregateTestReports -S

      - name: Upload test reports
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: test-reports
          path: |
            build/reports/aggregate-tests/
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: redis-kafka-connect
          path: core/redis-kafka-connect/build/confluent/redis-redis-kafka-connect-*.zip
# jobs:
#   build:
#     name: 'Build'
    # uses: redis-field-engineering/redis-github-workflows/.github/workflows/build.yml@main